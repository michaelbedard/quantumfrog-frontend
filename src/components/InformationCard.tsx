import {Coordinates} from "../pages/App";

interface InformationCardProps {
}

const InformationCard: ({props}: { props: InformationCardProps }) => React.JSX.Element = ({props} : {props : InformationCardProps}) => {

    return (
        <div>


        </div>
    )
}

export default InformationCard